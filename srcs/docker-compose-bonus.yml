services:
  nginx:
    build: ./requirements/nginx/.
    container_name: nginx
    environment:
      - NGINX_MODE=bonus
    restart: on-failure
    ports:
      - "443:443"
    volumes:
      - /home/gsoteldo/data/web:/var/www/html
    # networks:
    #   - inception-network
    # depends_on:
    #   - wordpress